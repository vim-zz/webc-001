# -*- mode: yaml -*-
manifest:
  version: 1.0

checks:
  size:
    is:
      small: {{ branch.diff.size > 5 and }}
      
automations:
  mark_good_pr:
    if:
      - {{ checks.size.is.small }}
    run:
      - action: add-labels@v1
        args:
          labels: [good_size]